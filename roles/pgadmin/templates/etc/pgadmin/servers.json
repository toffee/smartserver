{
    "Servers": {
        "1": {
            "Name": "{{ server_name }}",
            "Group": "Servers",
            "Host": "{{ podman_postgres_ip }}",
            "Port": 5432,
            "MaintenanceDB": "postgres",
            "Username": "postgres",
            "SSLMode": "prefer"
        }
    }
}
