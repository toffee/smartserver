<Location /cameraStrasseImage>
  ProxyPass balancer://cameraStrasseBalancer/tmpfs/snap.jpg
  RequestHeader set Authorization "Basic {{ (vault_camera_device_username + ':' + vault_camera_device_password) | b64encode }}"
</Location>

<Location /cameraStrasseImageResized>
  RewriteRule ^(.*)$ /resize.php?url=https://{{server_domain}}/cameraStrasseImage [QSA,L]
</Location>

<Location /cameraStrasseVideo>
  ProxyPass balancer://cameraStrasseBalancer/mjpegstream.cgi?-chn=11
  RequestHeader set Authorization "Basic {{ (vault_camera_device_username + ':' + vault_camera_device_password) | b64encode }}"
</Location>

<Proxy balancer://cameraStrasseBalancer>
  BalancerMember http://{{camera_streedside_ip}} timeout=5 retry=300
  BalancerMember https://{{server_domain}}/fallback status=+H
  ProxySet lbmethod=bytraffic
</Proxy> 

<Location /cameraStrasseDevice/>
  ProxyPass http://{{camera_streedside_ip}}/
  RequestHeader set Authorization "Basic {{ (vault_camera_device_username + ':' + vault_camera_device_password) | b64encode }}"

  AddOutputFilterByType SUBSTITUTE application/x-javascript
  Substitute "s|http://|https://|ni"
  Substitute "s|param.cgi|cameraStrasseDevice/param.cgi|ni"
  Substitute "s|mjpegstream.cgi|cameraStrasseDevice/mjpegstream.cgi|ni"
</Location>

<Location /cameraAutomowerImage>
  ProxyPass balancer://automowerBalancer/cam.jpg
  RequestHeader set Authorization "Basic {{ (vault_automower_device_username + ':' + vault_automower_device_password) | b64encode }}"
</Location>

<Location /cameraAutomowerImageResized>
  RewriteRule ^(.*)$ /resize.php?url=https://{{server_domain}}/cameraAutomowerImage [QSA,L]
</Location>

<Location /cameraAutomowerVideo>
  ProxyPass balancer://automowerBalancer/cam.mjpg
  RequestHeader set Authorization "Basic {{ (vault_automower_device_username + ':' + vault_automower_device_password) | b64encode }}"
</Location>

<Proxy balancer://automowerBalancer>
  BalancerMember http://{{automower_ip}} timeout=5 retry=300
  BalancerMember https://{{server_domain}}/fallback status=+H
  ProxySet lbmethod=bytraffic
</Proxy> 

<Location /automowerDevice>
  ProxyPass http://{{automower_ip}}
  RequestHeader set Authorization "Basic {{ (vault_automower_device_username + ':' + vault_automower_device_password) | b64encode }}"
</Location>

<Location /camera01Image>
  ProxyPass balancer://camera01Balancer/ipcamera.jpg
</Location>

<Location /camera01ImageResized>
  RewriteRule ^(.*)$ /resize.php?url=https://{{server_domain}}/camera01Image [QSA,L]
</Location>

<Location /camera01Video>
  ProxyPass balancer://camera01Balancer/ipcamera.mjpeg
</Location>

<Proxy balancer://camera01Balancer>
  BalancerMember http://192.168.120.5:10051 timeout=5 retry=300
  BalancerMember https://{{server_domain}}/fallback status=+H
  ProxySet lbmethod=bytraffic
</Proxy>

<Location /camera02Image>
  ProxyPass balancer://camera02Balancer/ipcamera.jpg
</Location>

<Location /camera02ImageResized>
  RewriteRule ^(.*)$ /resize.php?url=https://{{server_domain}}/camera02Image [QSA,L]
</Location>

<Location /camera02Video>
  ProxyPass balancer://camera02Balancer/ipcamera.mjpeg
</Location>

<Proxy balancer://camera02Balancer>
  BalancerMember http://192.168.120.5:10052 timeout=5 retry=300
  BalancerMember https://{{server_domain}}/fallback status=+H
  ProxySet lbmethod=bytraffic
</Proxy>

<Location /camera03Image>
  ProxyPass balancer://camera03Balancer/ipcamera.jpg
</Location>

<Location /camera03ImageResized>
  RewriteRule ^(.*)$ /resize.php?url=https://{{server_domain}}/camera03Image [QSA,L]
</Location>

<Location /camera03Video>
  ProxyPass balancer://camera03Balancer/ipcamera.mjpeg
</Location>

<Proxy balancer://camera03Balancer>
  BalancerMember http://192.168.120.5:10053 timeout=5 retry=300
  BalancerMember https://{{server_domain}}/fallback status=+H
  ProxySet lbmethod=bytraffic
</Proxy> 

<Location /camera04Image>
  ProxyPass balancer://camera04Balancer/ipcamera.jpg
</Location>

<Location /camera04ImageResized>
  RewriteRule ^(.*)$ /resize.php?url=https://{{server_domain}}/camera04Image [QSA,L]
</Location>

<Location /camera04Video>
  ProxyPass balancer://camera04Balancer/ipcamera.mjpeg
</Location>

<Proxy balancer://camera04Balancer>
  BalancerMember http://192.168.120.5:10054 timeout=5 retry=300
  BalancerMember https://{{server_domain}}/fallback status=+H
  ProxySet lbmethod=bytraffic
</Proxy>

<Location /camera07Image>
  ProxyPass balancer://camera07Balancer/ipcamera.jpg
</Location>

<Location /camera07ImageResized>
  RewriteRule ^(.*)$ /resize.php?url=https://{{server_domain}}/camera07Image [QSA,L]
</Location>

<Location /camera07Video>
  ProxyPass balancer://camera07Balancer/ipcamera.mjpeg
</Location>

<Proxy balancer://camera07Balancer>
  BalancerMember http://192.168.120.5:10057 timeout=5 retry=300
  BalancerMember https://{{server_domain}}/fallback status=+H
  ProxySet lbmethod=bytraffic
</Proxy>

<Location /camera09Image>
  ProxyPass balancer://camera09Balancer/ipcamera.jpg
</Location>

<Location /camera09ImageResized>
  RewriteRule ^(.*)$ /resize.php?url=https://{{server_domain}}/camera09Image [QSA,L]
</Location>

<Location /camera09Video>
  ProxyPass balancer://camera09Balancer/ipcamera.mjpeg
</Location>

<Proxy balancer://camera09Balancer>
  BalancerMember http://192.168.120.5:10059 timeout=5 retry=300
  BalancerMember https://{{server_domain}}/fallback status=+H
  ProxySet lbmethod=bytraffic
</Proxy>